# gradle

.gradle/
build/
out/
classes/

# 本机构建用的临时目录（便携 JDK / Gradle 缓存 / TMPDIR），绝不入库
/.jdk/
/.gradle-home/
/.tmp/
# 构建与反向验证时重定向出来的日志（曾经误提交过一次 .build-263.txt）
/.build-*.txt
/.reverse-*.txt
/.probe*.txt

# 本地配置（含 DeepSeek API Key），绝对不要提交
config/hxtranslate.json
run/config/hxtranslate.json
*.local.json

# eclipse

*.launch

# idea

.idea/
*.iml
*.ipr
*.iws

# vscode

.settings/
.vscode/
bin/
.classpath
.project

# macos

*.DS_Store

# fabric

run/

# 运行期产物：跑离线自检（./gradlew verifyCore）时 slf4j 会在项目根目录写出 logs/
logs/
crash-reports/

# 会话衔接提示词（本地草稿，不入库）

*衔接提示词*.txt

# java

hs_err_*.log
replay_*.log
*.hprof
*.jfr
